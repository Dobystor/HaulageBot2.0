# ============================================================
# Plantilla de variables de entorno para HaulageBot2.0
# Copiar a ".env" y ajustar los valores según el servidor:
#   cp .env.example .env
# ============================================================

# Puerto en el que se expone el bot en el host
HOST_PORT=5101

# --- Base de datos SQL Server (la que ya existe en el servidor) ---
# El bot crea/usa su propia base "haulages_bot" en esta instancia.
DB_SERVER=10.174.109.16
DB_NAME=haulages_bot
DB_USER=ejemplo
DB_PASSWORD=ejemplo

# --- Autenticación contra SmartFlow ---
API_URL=https://dispatch-01-sf.smartflow.com.mx
CLIENT_ID=private.networking.app
CLIENT_SECRET=UxwYJsELeTnSc2Zz642K
